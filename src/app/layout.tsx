import type { Metadata } from "next";
import { Outfit } from "next/font/google";
import "./globals.css";

const outfit = Outfit({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Jothi Computer Education - Best Computer Application Centre in Tirunelveli",
  description: "Join Jothi Computer Education for top-notch computer training in Tirunelveli. Courses: DCA, Python, Java, Full Stack, Data Science, and more.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={outfit.className}>{children}</body>
    </html>
  );
}
